{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Infinity Protocol: Auto-Verification (E2E)",
      "type": "shell",
      "command": "if [ -f package.json ]; then if grep -q '\"test:e2e\"' package.json; then npm run test:e2e; elif grep -q '\"test\"' package.json; then npm run test; else echo 'No test script found.'; fi; fi",
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true,
        "focus": false
      },
      "problemMatcher": []
    }
  ]
}
