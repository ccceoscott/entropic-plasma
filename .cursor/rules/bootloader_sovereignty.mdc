---
alwaysApply: true
description: BOOTLOADER SOVEREIGNTY - Next.js Setup, Providers, and Protocol Injection
globs: "**/layout.tsx, **/App.tsx, **/_app.tsx, package.json, README.md, MISSION_STATE.md"
---
# BOOTLOADER SOVEREIGNTY: INFINITY PROTOCOL v6.2.15

> **THE FOUNDATION**: This rule ensures every project is correctly initialized with the required React 19 / Next.js 15 providers and that the Infinity Protocol is active and healthy.

## 1. THE PRIME DIRECTIVE (Protocol Injection)
**IF** you are in a project that does **NOT** have a `.cursor/rules` directory OR a `MISSION_STATE.md`:
1.  **HALT** all other tasks.
2.  **NOTIFY** the user: "Infinity Protocol Missing. Initiating Sovereign Injection."
3.  **EXECUTE** the injection: `~/scripts/install_infinity_skills.sh .`
4.  **CREATE** `MISSION_STATE.md` if absent.

## 2. NEXT.JS 15 ROOT LAYOUT STANDARD
- **Constraint**: Must enforce `suppressHydrationWarning` on `<html>`.
- **Typography**: Inject primary Sans font at the root via CSS variables.

```tsx
// ✅ REQUIRED PATTERN: app/layout.tsx
import { Inter } from 'next/font/google';
import { Providers } from '@/components/Providers';
import { Toaster } from 'sonner';
import './globals.css';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });

export const metadata = {
  title: 'Infinity Protocol Sovereign App',
  description: 'Built on v6.2 Liquid Glass Standards.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning className={`${inter.variable} font-sans`}>
      <body className="antialiased bg-black text-white selection:bg-white/30">
        <Providers>
          {children}
          <Toaster position="bottom-right" theme="dark" richColors />
        </Providers>
      </body>
    </html>
  );
}
```

## 3. UNIVERSAL PROVIDERS (React 19 / Next.js 15)
- **Constraint**: Wrap all Client Contexts in a dedicated wrapper.
- **Hydration**: Use `next-themes` and `TanStack Query` with safe initialization.

```tsx
// ✅ REQUIRED PATTERN: components/Providers.tsx
'use client';

import { ThemeProvider } from 'next-themes';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { useState } from 'react';

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: { queries: { staleTime: 60 * 1000, retry: 1 } },
  }));

  return (
    <QueryClientProvider client={queryClient}>
      <ThemeProvider attribute="class" defaultTheme="dark" enableSystem disableTransitionOnChange>
        {children}
      </ThemeProvider>
    </QueryClientProvider>
  );
}
```

## 4. HEALTH ENFORCEMENT
**Pre-build hook logic in package.json**:
```javascript
// scripts/enforce-infinity.js
const { execSync } = require('child_process');
const fs = require('fs');

// Gate 1: Protocol presence
if (!fs.existsSync('.cursor/rules') || !fs.existsSync('MISSION_STATE.md')) {
  console.error('[FATAL] Infinity Protocol Not Detected! Run: dv upgrade');
  process.exit(1);
}

// Gate 2: Phase 43 Machine Laws (NODE_OPTIONS)
const nodeOpts = process.env.NODE_OPTIONS || '';
if (!nodeOpts.includes('max-old-space-size')) {
  console.error('[FATAL] NODE_OPTIONS not set. Add NODE_OPTIONS=--max-old-space-size=4096 to ~/.zshenv');
  process.exit(1);
}

console.log('✅ Infinity Protocol v6.2.15: HEALTH PASS');
```

## 5. REFACTORING LEGACY
- Prohibit Redux or Pages Router in greenfield projects.
- Migrate to Next.js App Router + Context/Zustand immediately.

## 6. PHASE 43 BOOTLOADER SOVEREIGNTY
On first entry to any workspace, the agent MUST perform this verification sequence BEFORE any other action:

```bash
# ⚠️ MANDATORY BOOT SEQUENCE (Phase 43)
echo "[INFINITY PROTOCOL v6.2.15] Boot Sequence Initiated..."

# 1. Protocol integrity
[ -f "MISSION_STATE.md" ] || { echo "❌ MISSING: MISSION_STATE.md"; exit 1; }
[ -f "GEMINI.md" ] && grep -q 'v6.2.15' GEMINI.md || echo "⚠️ WARNING: GEMINI.md drift detected. Run: dv fix-drift"

# 2. Phase 43 Machine Laws
[ -n "$NODE_OPTIONS" ] && echo "✅ NODE_OPTIONS: $NODE_OPTIONS" || echo "❌ NODE_OPTIONS MISSING — add to ~/.zshenv"
[ -n "$_JAVA_OPTIONS" ] && echo "✅ _JAVA_OPTIONS: $_JAVA_OPTIONS" || echo "❌ _JAVA_OPTIONS MISSING — Firebase Emulators will OOM"
[ "$NEXT_TELEMETRY_DISABLED" = "1" ] && echo "✅ Telemetry: KILLED" || echo "⚠️ NEXT_TELEMETRY_DISABLED not set"

# 3. MCP Watchdog
pgrep -f mcp_watchdog.sh > /dev/null && echo "✅ MCP Watchdog: ACTIVE" || echo "⚠️ MCP Watchdog not running"

echo "[INFINITY PROTOCOL v6.2.15] Boot Sequence Complete."
```
