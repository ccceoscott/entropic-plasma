---
description: Invoke the Capacitor Mobile Architect — Native Bridges and Deep Linking.
alwaysApply: false
---

# Workflow: Mobile Architect (Summoned)


## 🧠 Skill Ingestion (MANDATORY — Load Before Execution)
**Automatically ingest this skill** via `view_file` on its `SKILL.md` before proceeding:
1. `.agent/skills/capacitor-mobile-architect/SKILL.md` — iOS/Android deep linking, push notifications, native API, App Store compliance

## 🔐 SOVEREIGN UPGRADE GATE — MANDATORY

### Phase 0a — Protocol Sync
// turbo
```bash
bash ./scripts/session-proof.sh 2>&1
```

### Phase 0b — TypeScript Verification
// turbo
```bash
cd functions && PATH="/opt/homebrew/Cellar/node@22/22.22.0/bin:/opt/homebrew/bin:$PATH" NODE_OPTIONS=--max-old-space-size=4096 timeout 60 ./node_modules/.bin/tsc --noEmit --skipLibCheck 2>&1 | tail -15
```

### Phase 0c — DUAL PHASE SNAPSHOT
Extract `WORKSPACE_PHASE` from `MISSION_STATE.md`.

---


*The protocol expands into the palm of the hand.*

## Arcane Objective
You have summoned the **Capacitor Mobile Architect**. Use this workflow to manage native iOS/Android configurations and deep linking mechanics.

## Ritual Steps
1.  **Ingest Skill**: Automatically reading `capacitor-mobile-architect/SKILL.md`.
2.  **Native Check**: Audit `capacitor.config.ts` and `ios/` or `android/` folders.
3.  **Bridge Audit**: Verify Capacitor plugin usage and custom native listeners.

*The mobile realm bows to the protocol.*
