---
description: Scott's Standard Operating Protocols (God Mode)
---
# Scott's Antigravity Protocols

This workflow codifies the "God Mode" operating rules for Scott (Constant Concepts). Run this at the start of complex sessions or when switching contexts.

## 1. 🧠 Context Loading (Memory)
- [ ] Call `mcp_memory_read_graph` or ask "What is my current context?" to refresh memory of:
    - **Identity:** Scott (US Navy Vet, Agency Owner)
    - **Projects:** More Bass, Ai ZEN, SynergyFit
    - **Hardware:** Acura MDX, Pioneer DDJ-REV1/FLX4
    - **Stack:** Firebase, React, Vite, Expo

## 2. 📚 Documentation Policy (Context7)
- [ ] **BEFORE** writing any code for React, Expo, or Firebase:
    - Call `context7` (specifically `@upstash/context7-mcp` via `context7` tool).
    - Query: "latest [library] documentation for [feature]"
    - **DO NOT** rely on internal training data for these libraries.

## 3. 🛡️ Data Validation (Firebase)
- [ ] **BEFORE** guessing a Firestore schema:
    - Use `firebase` tool to inspect *live data*.
    - Example: `firebase-mcp-server_firestore_get_documents` or `list_collections`.
    - **NEVER** assume schema from old code files.

## 4. 🦁 Web Intelligence (Brave)
- [ ] **IF** the user mentions:
    - A specific error message
    - A competitor
    - A "current event" (tech release, date)
    - **THEN** immediately use `brave-search` to get ground truth.

## 5. 🎨 Aesthetic Standard
- [ ] All UI work must match the **Premium Cyber-Dark** aesthetic.
- [ ] No "default blue" links or unstyled buttons.
- [ ] Use `generate_image` for placeholders (don't use grey boxes).
