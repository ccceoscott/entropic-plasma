---
description: Invoke the IDE Architect — Performance, MCP Health, and Maintenance.
alwaysApply: false
---

# Workflow: IDE Architect (Summoned)


## 🧠 Skill Ingestion (MANDATORY — Load Before Execution)
**Automatically ingest this skill** via `view_file` on its `SKILL.md` before proceeding:
1. `.agent/skills/antigravity-ide-architect/SKILL.md` — MCP server health, browser subagent orchestration, IDE performance

## 🔐 SOVEREIGN UPGRADE GATE — MANDATORY

### Phase 0a — Protocol Sync
// turbo
```bash
bash ./scripts/session-proof.sh 2>&1
```

### Phase 0b — TypeScript Verification
// turbo
```bash
cd functions && PATH="/opt/homebrew/Cellar/node@22/22.22.0/bin:/opt/homebrew/bin:$PATH" NODE_OPTIONS=--max-old-space-size=4096 timeout 60 ./node_modules/.bin/tsc --noEmit --skipLibCheck 2>&1 | tail -15
```

### Phase 0c — DUAL PHASE SNAPSHOT
Extract `WORKSPACE_PHASE` from `MISSION_STATE.md`.

---


*The machine itself shall be optimized.*

## Arcane Objective
You have summoned the **Antigravity IDE Architect**. Use this workflow to troubleshoot performance stalls, purge zombie processes, or manage MCP configurations.

## Ritual Steps
1.  **Ingest Skill**: Automatically reading `antigravity-ide-architect/SKILL.md`.
2.  **Heal**: Run `dv health` and perform needed purges (browser recordings, logic bloat).
3.  **Audit**: Check MCP server logs for timeout patterns.

*The gears of the void turn smoothly once more.*
