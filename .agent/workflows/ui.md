---
description: Invoke the Liquid Glass UI Scaffolder — Premium Aesthetic and Component Engineering.
alwaysApply: false
---

# Workflow: UI Architect (Summoned)


## 🧠 Skill Ingestion (MANDATORY — Load Before Execution)
**Automatically ingest this skill** via `view_file` on its `SKILL.md` before proceeding:
1. `.agent/skills/liquid-glass-ui/SKILL.md` — Premium dark-mode components, glassmorphism, Framer Motion, fluid typography

## 🔐 SOVEREIGN UPGRADE GATE — MANDATORY

### Phase 0a — Protocol Sync
// turbo
```bash
bash ./scripts/session-proof.sh 2>&1
```

### Phase 0b — TypeScript Verification
// turbo
```bash
cd functions && PATH="/opt/homebrew/Cellar/node@22/22.22.0/bin:/opt/homebrew/bin:$PATH" NODE_OPTIONS=--max-old-space-size=4096 timeout 60 ./node_modules/.bin/tsc --noEmit --skipLibCheck 2>&1 | tail -15
```

### Phase 0c — DUAL PHASE SNAPSHOT
Extract `WORKSPACE_PHASE` from `MISSION_STATE.md`.

---


*By the Eye of Zoltan, the UI shall be refined.*

## Arcane Objective
You have summoned the **Liquid Glass UI Scaffolder**. Use this workflow to generate or modify high-fidelity components using Shadcn, Framer Motion, and Liquid Glass aesthetics.

## Ritual Steps
1.  **Ingest Skill**: Automatically reading `liquid-glass-ui/SKILL.md`.
2.  **Context Audit**: Analyzing current `src/components` and `tailwind.config.ts`.
3.  **Aesthetic Scan**: Determining if `dv aesthetic` should be run to verify baseline.
4.  **Execute**: Implement the requested visual splendor.

*The shards of light shall dance upon your screen.*
