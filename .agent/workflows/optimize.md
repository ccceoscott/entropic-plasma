---
description: System Optimization Audit — Core Web Vitals, Payload Tree-Shaking, and DB Query Tuning.
alwaysApply: false
---

# ⚡ /optimize — The Velocity Engine (v12.0)

⚡ **MANDATE**: Execute this ritual to eradicate performance bottlenecks, optimize Core Web Vitals, implement tree-shaking, and index database queries.

## 🧠 Skill Ingestion
**Automatically ingest this Domain Bundle**:
1. `!hammer` — Sovereign Hammer (Ops, Testing, Execution)

---

## 🔍 THE OPTIMIZATION PATH
1. **Core Web Vitals**: Run local build analytics to target LCP < 2.5s, zero CLS (layout shifts), and FID < 100ms.
2. **Bundle & Tree-Shaking**: Analyze React/Next.js payloads. Purge unused libraries and enforce dynamic imports (`next/dynamic` or React `lazy`) for non-critical chunking.
3. **Asset Sovereignty**: Ensure all imagery uses modern optimization components (`next/image`) with strict WebP/AVIF formatting and explicit aspect ratios.
4. **Database & Cold Starts**: Scour Firestore for unindexed composite queries. Refactor unbounded list queries to use `.limit()` and cursors. Check Cloud Functions logs for cold starts exceeding 3s.

Declare: `✅ [OPTIMIZATION COMPLETE] | Core Web Vitals secured. Payload minimized.`
